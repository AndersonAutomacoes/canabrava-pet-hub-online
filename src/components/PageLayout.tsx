
import React from 'react';
import { PageLayout as Layout } from '@/components/layout/PageLayout';

// Re-export the PageLayout component for backward compatibility
const PageLayout = Layout;

export default PageLayout;
