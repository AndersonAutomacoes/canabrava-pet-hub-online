
import React from 'react';
import { SearchBar as SearchBarComponent } from '@/components/ui/search-bar';

// Re-export the SearchBar component for backward compatibility
const SearchBar = SearchBarComponent;

export default SearchBar;
